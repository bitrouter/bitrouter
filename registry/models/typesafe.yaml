- id: typesafe/jev-1.13
  name: Jev 1.13
  description: Typed evaluation model. Routing requires the native System One format extension in the opt-in evaluation host; stock bro does not expose /v1/evaluate.
  operations:
    - evaluate
  input_modalities:
    - text
