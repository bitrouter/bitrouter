//! ACP wire-format types for serialization at API boundaries.
//!
//! Parallel to `api::mcp::types` and `api::openai::chat::types`.
//! These represent the JSON-RPC shapes used by ACP-compatible agents.
//! Currently a placeholder — concrete types will be added when an HTTP
//! surface for agents is introduced.
