//! Conversion utilities between core agent types and ACP wire types.
//!
//! Parallel to `api::mcp::convert` and `api::openai::chat::convert`.
//! Currently a placeholder — conversion functions will be added when
//! the ACP wire types in [`types`](super::types) are populated.
