protocol: acp
binary: copilot
args:
  - "--acp"
  - "--stdio"
distribution:
  - npx:
      package: "@github/copilot"
      args:
        - "--acp"
# No routing: Copilot uses GitHub OAuth with no base URL override.
