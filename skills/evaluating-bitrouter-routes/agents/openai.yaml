interface:
  display_name: "Evaluate BitRouter Routes"
  short_description: "Build conservative route evidence packets"
  default_prompt: "Use $evaluating-bitrouter-routes to turn verifier outcomes into conservative BitRouter route evidence."
